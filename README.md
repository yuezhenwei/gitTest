# gitTest

test for git using

1.change for commit

2.

```bash
git config --global alias.pushall '!git push github && git push gitee'
```


3.

git remote set-url --add --push origin git@gitee.com:kid-kid/gitTest.git

yuezhenwei  …\gitTest   PLY   ♥ 21:23  git remote -v
origin  git@github.com:yuezhenwei/gitTest.git (fetch)
origin  git@gitee.com:kid-kid/gitTest.git (push)
origin  git@gitee.com:kid-kid/gitTest.git (push)
